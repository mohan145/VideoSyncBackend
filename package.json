{
  "name": "video_sync_backend",
  "version": "1.0.0",
  "description": "backend with websockets for video sync",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "nodemon": "^2.0.3",
    "socket.io": "^2.3.0"
  }
}
